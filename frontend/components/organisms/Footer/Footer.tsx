import { Box } from "@mui/material";

const Footer: React.FC = () => {
  return (
    <Box sx={{ backgroundColor: "#ADD8E6", padding: "1rem", textAlign: "center" }}>
      <p>hoge</p>
    </Box>
  );
};

export default Footer;
